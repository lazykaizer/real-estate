import type { Metadata } from "next";

export const metadata: Metadata = { title: "Buy Property" };

export default function BuyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
